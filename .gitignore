node_modules/
dist/
.env
.env.local
server/data/*.json
!server/data/.gitkeep
*.log
.DS_Store
# Test scratch store. Anchored patterns only match at the repo root, so match at any depth.
**/test/.data/
