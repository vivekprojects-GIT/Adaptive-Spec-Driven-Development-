---
name: bmad-testworkflow
description: 'ASDD test fixture — not a real BMAD skill. Produces the "spine", with it''s quoting exercised.'
---

# Test workflow (fixture)

## Overview

A workflow fixture installed the way Claude Code installs BMAD skills — under .claude/skills —
so the loader's fallback from the manifest path is exercised.
