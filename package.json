{
  "name": "adaptive-spec-driven-development",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ASDD - Adaptive Spec Driven Development: a migration-adaptive AI control plane built with the ASDD method.",
  "workspaces": ["server", "web"],
  "bin": { "asdd": "server/src/cli.js" },
  "engines": { "node": ">=20.6" },
  "scripts": {
    "setup": "npm install",
    "postinstall": "npm run build || exit 0",
    "dev": "concurrently -n server,web -c cyan,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev",
    "build": "npm --workspace web run build",
    "start": "npm --workspace server run start",
    "seed": "npm --workspace server run seed",
    "test": "npm --workspace server run test",
    "asdd": "node server/src/cli.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
