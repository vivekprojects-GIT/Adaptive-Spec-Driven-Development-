# Optional. Without a key the platform runs its deterministic rule engine (fully offline).
# With a key, Discovery / Agent Factory / Guardrail Designer additionally consult an LLM.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
PORT=5174
